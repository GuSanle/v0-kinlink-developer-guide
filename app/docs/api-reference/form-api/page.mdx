

import { CodeBlock } from "@/components/code-block";

# 表单API

<div className="mt-2 text-lg text-muted-foreground">
  用于操作表单字段和值的函数。
</div>

表单API（`kinlink.formApi`）提供了用于操作表单字段和值的函数。这包括获取和设置字段值、隐藏和显示字段、验证字段等功能。

## 字段值操作

### `getFieldValue(code)`

获取特定字段的值。

<CodeBlock
  code={`// 获取'name'字段的值
const name = kinlink.formApi.getFieldValue('name');
console.log(name); // 输出: "张三"`}
  language="javascript"
/>

### `setFieldValue(code, value)`

设置特定字段的值。

<CodeBlock
  code={`// 设置'name'字段的值
kinlink.formApi.setFieldValue('name', '李四');`}
  language="javascript"
/>

### `getAllValues()`

获取表单中所有字段的值。

<CodeBlock
  code={`// 获取所有表单值
const values = kinlink.formApi.getAllValues();
console.log(values); // 输出: { name: "张三", email: "zhang@example.com", ... }`}
  language="javascript"
/>

### `setFieldsValue(values)`

一次性设置多个字段的值。

<CodeBlock
  code={`// 设置多个字段值
kinlink.formApi.setFieldsValue({
  name: '李四',
  email: 'li@example.com',
  age: 30
});`}
  language="javascript"
/>

## 字段可见性

### `hideField(code)`

完全隐藏字段（字段的值不会被提交）。

<CodeBlock
  code={`// 隐藏'additionalInfo'字段
kinlink.formApi.hideField('additionalInfo');`}
  language="javascript"
/>

### `showField(code)`

显示先前隐藏的字段。

<CodeBlock
  code={`// 显示'additionalInfo'字段
kinlink.formApi.showField('additionalInfo');`}
  language="javascript"
/>

### `visuallyHideField(code)`

视觉上隐藏字段但在表单提交中保留其值。

<CodeBlock
  code={`// 视觉上隐藏'hiddenId'字段
kinlink.formApi.visuallyHideField('hiddenId');`}
  language="javascript"
/>

### `getFieldState(code)`

获取字段的当前状态。

<CodeBlock
  code={`// 获取'name'字段的状态
const state = kinlink.formApi.getFieldState('name');
console.log(state); // 输出: { visible: true, disabled: false, excludeFromSubmit: false }`}
  language="javascript"
/>

## 字段启用/禁用

### `disableField(code)`

禁用字段（使其为只读）。

<CodeBlock
  code={`// 禁用'readOnlyField'字段
kinlink.formApi.disableField('readOnlyField');`}
  language="javascript"
/>

### `enableField(code)`

启用先前禁用的字段。

<CodeBlock
  code={`// 启用'readOnlyField'字段
kinlink.formApi.enableField('readOnlyField');`}
  language="javascript"
/>

## 验证

### `addFieldValidator(code, validator)`

为字段添加自定义验证器函数。

<CodeBlock
  code={`// 为'email'字段添加验证器
kinlink.formApi.addFieldValidator('email', (value) => {
  if (!value) return;
  if (!/^\\\\w+@\\\\w+\\\\.\\\\w+$/.test(value)) {
    return '请输入有效的电子邮件地址';
  }
  return undefined; // 验证通过
});`}
  language="javascript"
/>

### `removeFieldValidator(code)`

从字段中移除自定义验证器。

<CodeBlock
  code={`// 从'email'字段移除验证器
kinlink.formApi.removeFieldValidator('email');`}
  language="javascript"
/>

### `validateField(code, value)`

验证特定字段值。

<CodeBlock
  code={`// 验证'email'字段
const error = kinlink.formApi.validateField('email', 'invalid-email');
console.log(error); // 输出: "请输入有效的电子邮件地址"`}
  language="javascript"
/>

### `validateForm()`

验证整个表单。

<CodeBlock
  code={`// 验证整个表单
const result = kinlink.formApi.validateForm();
console.log(result); // 输出: { errors: { email: "请输入有效的电子邮件地址" }, isValid: false }`}
  language="javascript"
/>

### `setFieldError(code, errorMsg)`

为字段设置错误消息。

<CodeBlock
  code={`// 为'email'字段设置错误消息
kinlink.formApi.setFieldError('email', '此电子邮件已注册');`}
  language="javascript"
/>

### `clearFieldError(code)`

清除字段的错误消息。

<CodeBlock
  code={`// 清除'email'字段的错误消息
kinlink.formApi.clearFieldError('email');`}
  language="javascript"
/>

### `setFieldsErrors(fieldErrors)`

为多个字段设置错误消息。

<CodeBlock
  code={`// 为多个字段设置错误消息
kinlink.formApi.setFieldsErrors({
  email: '此电子邮件已注册',
  password: '密码必须至少8个字符'
});`}
  language="javascript"
/>

### `setFieldLabelStyle(code, styleObject)`

设置字段标签的CSS样式。

<CodeBlock
  code={`// 设置'name'字段标签的样式
kinlink.formApi.setFieldLabelStyle('name', {
  color: 'red',
  fontWeight: 'bold'
});`}
  language="javascript"
/>

### `getFieldLabelStyle(code)`

获取字段标签的当前自定义样式。

<CodeBlock
  code={`// 获取'name'字段标签的样式
const style = kinlink.formApi.getFieldLabelStyle('name');
console.log(style); // 输出: { color: "red", fontWeight: "bold" }`}
  language="javascript"
/>

### `resetFieldLabelStyle(code)`

将字段标签的样式重置为默认值。

<CodeBlock
  code={`// 重置'name'字段标签的样式
kinlink.formApi.resetFieldLabelStyle('name');`}
  language="javascript"
/>

### `setFieldsLabelStyles(stylesMap)`

设置多个字段标签的CSS样式。

<CodeBlock
  code={`// 设置多个字段标签的样式
kinlink.formApi.setFieldsLabelStyles({
  name: { color: 'red', fontWeight: 'bold' },
  email: { color: 'blue', fontStyle: 'italic' }
});`}
  language="javascript"
/>

### `setFieldComponentStyle(code, styleObject)`

设置字段组件（输入框、选择框等）的CSS样式。

<CodeBlock
  code={`// 设置'name'字段组件的样式
kinlink.formApi.setFieldComponentStyle('name', {
  backgroundColor: '#f0f0f0',
  padding: '10px'
});`}
  language="javascript"
/>

### `getFieldComponentStyle(code)`

获取字段组件的当前自定义样式。

<CodeBlock
  code={`// 获取'name'字段组件的样式
const style = kinlink.formApi.getFieldComponentStyle('name');
console.log(style); // 输出: { backgroundColor: "#f0f0f0", padding: "10px" }`}
  language="javascript"
/>

### `resetFieldComponentStyle(code)`

将字段组件的样式重置为默认值。

<CodeBlock
  code={`// 重置'name'字段组件的样式
kinlink.formApi.resetFieldComponentStyle('name');`}
  language="javascript"
/>

### `setFieldsComponentStyles(stylesMap)`

设置多个字段组件的CSS样式。

<CodeBlock
  code={`// 设置多个字段组件的样式
kinlink.formApi.setFieldsComponentStyles({
  name: { backgroundColor: '#f0f0f0', padding: '10px' },
  email: { border: '1px solid blue' }
});`}
  language="javascript"
/>

## 消息通知

### `showSuccess(message, duration)`

显示成功消息。

<CodeBlock
  code={`// 显示成功消息
kinlink.formApi.showSuccess('操作成功完成！');`}
  language="javascript"
/>

### `showError(message, duration)`

显示错误消息。

<CodeBlock
  code={`// 显示错误消息
kinlink.formApi.showError('发生错误，请重试。');`}
  language="javascript"
/>

### `showInfo(message, duration)`

显示信息消息。

<CodeBlock
  code={`// 显示信息消息
kinlink.formApi.showInfo('这是一条信息提示。');`}
  language="javascript"
/>

### `showWarning(message, duration)`

显示警告消息。

<CodeBlock
  code={`// 显示警告消息
kinlink.formApi.showWarning('请注意，此操作无法撤销。');`}
  language="javascript"
/>

## 表单控制

### `submit()`

以编程方式提交表单。

<CodeBlock
  code={`// 提交表单
kinlink.formApi.submit();`}
  language="javascript"
/>

### `resetForm()`

重置表单到其初始状态。

<CodeBlock
  code={`// 重置表单
kinlink.formApi.resetForm();`}
  language="javascript"
/>

### `getFormElement()`

获取表单的根HTML元素。

<CodeBlock
  code={`// 获取表单元素
const formElement = kinlink.formApi.getFormElement();
console.log(formElement); // 输出: <form>...</form>`}
  language="javascript"
/>

### `getFieldElement(fieldCode)`

获取特定字段的HTML元素。

<CodeBlock
  code={`// 获取'name'字段的元素
const fieldElement = kinlink.formApi.getFieldElement('name');
console.log(fieldElement); // 输出: <input type="text" ...>`}
  language="javascript"
/>

### `hideKintoneLabel(fieldCode)`

隐藏指定字段的Kintone原生标签。

<CodeBlock
  code={'// 隐藏字段\'fieldCode\'的原生Kintone标签\nkinlink.formApi.hideKintoneLabel(\'fieldCode\');'}
  language="javascript"
/>

### `showKintoneLabel(fieldCode)`

显示指定字段的Kintone原生标签（如果之前被隐藏）。

<CodeBlock
  code={'// 显示字段\'fieldCode\'的原生Kintone标签\nkinlink.formApi.showKintoneLabel(\'fieldCode\');'}
  language="javascript"
/>

## 参数

- `url` `{string}`
  目标外部系统的完整URL（如第三方REST API地址）
- `method` `{string}`
  HTTP方法（如'GET', 'POST', 'PUT', 'DELETE'等）
- `headers` `{object}`
  请求头对象，键值对形式（如 `{\'Content-Type\': \'application/json\'}`）
- `body` `{object|string|undefined}`
  请求体内容。对于POST/PUT等方法，通常为对象（会自动序列化为JSON），也可为字符串或undefined
